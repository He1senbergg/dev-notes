# 1. 进文件

```
vi 文件名
```

# 2. 删除内容

## 2.1删除所有内容

### 2.1.1 命令1

```
:1,$d
```

- 1,$ 表示文件的第一行到最后一行
- d 是删除操作

### 2.1.2 命令2

```
gg
dG
```

- 按下 gg 将光标移动到文件的第一行
- 按下 dG，这将从光标所在行（即第一行）开始删除直到文件的末尾

## 2.2 删除部分行

删除一行：

在命令模式下将光标移至要删除的行位置，按下dd

删除n行：

假设要删除5行，在命令模式下将光标移至要删除的行的开始位置，按下5dd

# 3. 设置utf-8

```
:set encoding=utf-8
```

# 4. 设置行号

```
set nu
```

#vi #VI #utf-8 #encoding #删除n行 #vi进文件 #vi文件