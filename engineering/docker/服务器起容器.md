# 1. 带卡服务器起个用卡的容器

```
docker run  -itd --name wty_11 --privileged \   #考虑下是否需要起特权
--device=/dev/davinci6 \
--device=/dev/davinci_manager \
--device=/dev/devmm_svm \
--device=/dev/hisi_hdc \
-v /usr/local/dcmi:/usr/local/dcmi \
-v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi \
-v /usr/local/Ascend/driver:/usr/local/Ascend/driver \
-v /usr/local/Ascend/driver/lib64/common:/usr/local/Ascend/driver/lib64/common \
-v /usr/local/Ascend/driver/lib64/driver:/usr/local/Ascend/driver/lib64/driver \
-v /usr/local/Ascend/driver/version.info:/usr/local/Ascend/driver/version.info \
-v /etc/ascend_install.info:/etc/ascend_install.info \ 
-v /etc/vnpu.cfg:/etc/vnpu.cfg \
-v /home/usrname:/home/usrname \
4a757d814203 /bin/bash  # 镜像id：4a757d814203，
```

```
docker run -itd --name ttorchnpu --privileged \
  --device=/dev/davinci6 \
  --device=/dev/davinci_manager \
  --device=/dev/devmm_svm \
  --device=/dev/hisi_hdc \
  -v /usr/local/Ascend/driver:/usr/local/Ascend/driver \
  -v /home/usrname:/home/usrname \
  -v /usr/local/dcmi:/usr/local/dcmi \
  -v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi \
  -v /usr/local/Ascend/driver:/usr/local/Ascend/driver \
  -v /usr/local/Ascend/driver/lib64/common:/usr/local/Ascend/driver/lib64/common \
  -v /usr/local/Ascend/driver/lib64/driver:/usr/local/Ascend/driver/lib64/driver \
  -v /usr/local/Ascend/driver/version.info:/usr/local/Ascend/driver/version.info \
  -v /etc/ascend_install.info:/etc/ascend_install.info \
  -v /etc/vnpu.cfg:/etc/vnpu.cfg \
  -v /home/c00937227:/home/c00937227 \
  98228449aa9c /bin/bash
```

# 2. 一般的容器

```
docker run -itd --name torchnpu \
  -v /home/huawei/cfm/container:/home/huawei/cfm/container \
  16c8d445b70a /bin/bash
```

#docker #起容器